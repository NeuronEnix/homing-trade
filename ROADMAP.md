# ROADMAP — homing-trade

Autonomous crypto-futures paper-trading bot (Python 3.12, stdlib-first; optional deps: `anthropic`, `requests`).

**North star:** profitability — but NOT a near-term gate. Right now the order is: clean structure → total observability → UI/management/always-on → the autonomous learn→correct loop. Everything is paper-money by default; `LiveBroker` stays `dry_run` until an explicit human gate.

How to read this file: phases are in priority order (Phase 1 = most urgent). Check boxes as tasks land. Keep the `Progress:` line accurate so the GitHub board and this file agree. The user will not review code line-by-line and may merge on trust, so every PR must keep tests green and be fully self-explanatory.

---

## Phase 1 — Structural foundation (kill the god-files, harden module boundaries)
Goal: make the autonomous loop safe to operate and later self-modify by giving every concern a sharp module boundary.

- [x] Introduce a `repository.py` layer that wraps `db.py`: typed read/write methods so `engine.py`, `web.py`, `report.py`, `backtest.py`, `daemon.py` stop importing `Database` and issuing raw SQL directly (audit flagged this 5-way coupling). _(repository.py wraps Database; `report.py` + `engine.run` + `backtest.py` + `web.py` migrated; only `repository.py` imports `Database` now — `daemon.py` never used raw SQL)_
- [x] Define a `Ledger` ABC (new `ledger_base.py`) and make both `ledger.MemoryLedger` and the SQLite-backed repository implement it, replacing the current duck-typed interface. _(ledger_base.Ledger; MemoryLedger + Repository both implement it)_
- [x] Decompose `engine.py` (228 lines, fan-out 9): extract `SkillRunner` (build/run skills), `PositionManager` (`_open_position`/`_close_position`/stop/liquidation), and keep `engine.run` as a thin orchestration loop. `process_tick` should call into these, not inline the logic. _(PositionManager (open/close/manage_risk) + Advisor (sizing) + SkillRunner (build/run skills + trade alerts); engine.run is now a thin fetch/sleep loop)_
- [x] Split decision-logic (`Strategy.on_candle` → action+confidence) from execution-plumbing (leverage, `risk_pct`, `stop_pct`, sizing) so a skill never reaches into `cfg` for sizing; introduce a small `Advisor`/sizing helper called by `PositionManager`. _(advisor.Advisor owns sizing; PositionManager.open uses it)_
- [x] Extract the embedded HTML in `web.py` (335 lines) into a `web_assets/` template + a thin `build_state()` that calls the repository, not raw queries. _(DASHBOARD_HTML → web_assets/dashboard.html, loaded at import; build_state already on Repository)_
- [x] Add `PRAGMA`/index review in the repository: indexes on `decision_log(strategy, ts)`, `llm_responses(strategy, ts)`, `trades(strategy, ts)`, `trades(position_id)` for cheap reflection joins (keep existing WAL). _(db.py MIGRATIONS v1)_
- [x] Add `schema_version` to the `state` table + a tiny forward-only `migrate()` run at `Database.__init__`; every schema change bumps it (foundation for safe self-modification later). _(db.py `_migrate()` + `SCHEMA_VERSION`)_
- [x] Keep all of the above behind passing tests: extend `test_engine.py`, `test_db.py`, `test_web.py`; add `test_repository.py`. _(added test_repository, test_ledger_base, test_advisor, test_position_manager, test_skill_runner; extended test_db/test_web — full suite green throughout)_

Progress: 8/8

---

## Phase 2 — Total observability (complete SQLite action ledger + read-only self-query layer)
Goal: every action the bot takes (signal, open, close, fill, fee, P&L, AI rationale, error, risk veto) is one queryable row; the AI can ask "how did I do" through a safe read-only layer.

- [x] Extend `decision_log` with: `decision_id TEXT` (UUID), `prompt_version TEXT`, `playbook_version TEXT`, `regime TEXT`, `realized_vol REAL`, `intended_action TEXT`, `taken_action TEXT`, `rejection_rationale TEXT` (why a `DailyRiskGuard` veto blocked it). Tag at decision time, never re-derived. _(columns in v2; all populated in process_tick: `decision_id`/`intended_action`/`taken_action`/`rejection_rationale` + `regime`/`realized_vol`, and now `prompt_version`/`playbook_version` from the AI trader's Signal.meta (PR #44 — the playbook system))_
- [x] Add `prompt_hash` + a `prompt_version` to `llm_responses` and store `next_check_in_sec` / `requested_charts` the model returned, so a decision is fully replayable (the model already emits these in `llm_trader.py`). _(columns in v2; `next_check_in_sec` + `requested_charts` populated in process_tick; `prompt_version` + `prompt_hash` (logical fingerprint of system+user-context) now emitted by LlmTrader and persisted (PR #44))_
- [x] Record fills/fees explicitly: ensure every `_open_position`/`_close_position` writes `fill price vs decision price` (slippage) and fee into `trades` (most fields exist; add `decision_price`/`slippage`). _(columns in v2; `PositionManager.open`/`close` now record `decision_price` + `slippage` on every fill, across all Ledger backends)_
- [x] Add a `risk_events` table written by `DailyRiskGuard` (`can_open` veto, kill-switch trip) so vetoes are observable, not silent. _(table + accessors in v2; veto wired in `PositionManager.open`, kill-switch trip wired in `engine.run`)_
- [x] Create a forward-only `regimes` table `(pair, interval, time, regime, adx, ema_slope, realized_vol)` computed at decision time; reuse/extend `_tf_summary` in `llm_trader.py` and a new ADX helper in `skills/indicators.py`. _(regimes table in migration v3; `classify_regime` computed once per tick in process_tick → upserted to `regimes` + each `decision_log` row tagged with regime + realized_vol)_
- [x] Add a denormalized `trade_outcomes` view/table joining open→close `trades` by `position_id`, carrying `decision_id`, entry/exit price+ts, slippage, fees, `realized_pnl`, `pnl_pct`, `mae`, `mfe`, `holding_period_ms`, `exit_reason`, `regime_at_entry`, `prediction_correct`. This single row is what reflection reads. _(table v4 + `rebuild_trade_outcomes` builder (open→close join) + reader; all fields populated: core + `exit_reason` + `decision_id` + `regime_at_entry` + `prediction_correct` (mechanical directional) + `mae`/`mfe` (intra-trade excursion from the candle path, wall-clock→bar-open window snapping + invariant clamp). Outcome→decision→rationale traceable. Production rebuild-with-pair/interval call-site lands with the Phase-4 reflection consumer)_
- [x] Enforce an outcome embargo: outcome columns carry `realized_at_ts`; the self-query layer only exposes them where `realized_at_ts <= now` (prevents the Oracle Fallacy). _(realized_at_ts populated; `trade_outcomes(as_of)` reader + `SelfQuery.outcomes`/`regime_performance`/`exit_reason_breakdown`/`directional_accuracy` all pass `as_of` → embargo enforced at the self-query layer)_
- [x] Build `selfquery.py`: a strictly read-only query API (no writes, parameterized, whitelisted tables) the AI calls for "win_rate / profit_factor / per-regime / per-variant" — wrap `metrics.py` (`sharpe`, `win_rate`, `profit_factor`, `max_drawdown`). _(read-only `SelfQuery`: win_rate/profit_factor/sharpe/drawdown/expectancy + risk-event counts + intended-vs-taken decision breakdown; plus per-regime / per-exit-reason / directional-accuracy attribution over trade_outcomes, embargo-aware)_
- [x] Keep the audit-truth tables (`wallets`, `positions`, `trades`, `equity`, `candles`) machine-written only; only `decision_log`, `llm_responses`, `reflections`, `playbooks` may carry model-authored text (Hierarchy of Truth). _(`AUDIT_TRUTH_TABLES`/`MODEL_AUTHORED_TABLES` in db.py — complete + disjoint classification of every table, asserted by `test_hierarchy_of_truth` (fails the suite if a new table is added unclassified); `docs/hierarchy-of-truth.md` documents the rationale, per-table classification, read path + enforcement)_
- [x] Tests: `test_selfquery.py` (read-only enforced, embargo enforced), `test_trade_outcomes.py`, extend `test_db.py`/`test_llm_persistence.py`. _(all present + green: selfquery read-only spy + embargo, trade_outcomes (join/exit_reason/decision+regime/prediction_correct/mae+mfe/embargo), extended test_db migrations, test_llm_persistence replay fields)_

Progress: 10/10 _(v2–v6 schema; decision provenance + regime/vol tagging + risk_events + selfquery (incl. per-regime/per-exit/directional attribution) + llm replay fields + trade slippage + regimes table + trade_outcomes fully enriched (exit_reason/decision_id/regime_at_entry/prediction_correct/mae/mfe) + outcome embargo + Hierarchy-of-Truth classification/doc + Phase-2 test coverage all wired. The last two boxes (#1 decision_log prompt/playbook tags, #2 llm_responses prompt_version/prompt_hash) closed by PR #44 when the playbook system landed — Phase 2 complete)_

---

## Phase 3 — UI visibility, full management, and always-on
Goal: a dashboard that shows per-strategy & per-AI leaderboard + the brain-log (saw/predicted/why) + a proposal/approval queue + every control, and a supervisor that survives restarts.

- [x] Per-strategy & per-AI leaderboard in `web.build_state()`: balance, equity curve, win rate, profit factor, max drawdown, open position, last action — sourced from the repository, not raw queries. _(build_state enriches each item via read-only SelfQuery.leaderboard — rank/trades/win_rate/profit_factor/max_drawdown/expectancy/sharpe/realized_pnl/last_action + a 40-point equity_curve; list sorted by rank; dashboard renders a metrics grid + SVG equity sparkline per card; profit_factor inf→null JSON-safe)_
- [x] Brain-log panel: render `llm_responses` (observation / prediction / rationale / confidence / next_check_in_sec / error) per AI from `recent_llm_responses`. _(build_state returns `brain_log` — 30 most-recent responses across all AIs, newest first, trimmed to replayable fields (raw excluded); dashboard renders a scrollable panel: AI+model, time·action·confidence·next-check-in, error, and saw/predicts/why — read-only, null-safe)_
- [x] Per-regime and per-variant breakdown panel reading the Phase-2 `trade_outcomes`/`regimes` tables. _(build_state returns regime_breakdown + exit_breakdown via read-only SelfQuery.regime_performance/exit_reason_breakdown; dashboard renders "by regime" (trades/win%/total+avg pnl) and "by exit reason" tables. Also wired the production trade_outcomes rebuild into process_tick (no-op on backtest ledger) — MAE/MFE now populates live. Per-variant A/B attribution deferred to Phase-7 experiments)_
- [x] Proposal/approval queue UI (reads Phase-4 `proposals` table): list pending param/prompt/playbook proposals with Approve / Reject buttons posting to a new `/api/proposal` endpoint (mirror the existing `/api/control` + `/api/close` pattern in `web.py`). _(build_state surfaces `proposals` (recent_proposals, payload parsed); dashboard renders a "proposal queue" panel with Approve/Reject buttons; POST /api/proposal → Controller.decide_proposal: approve runs decide_proposal + ProposalApplier.apply (playbook auto-applies, others stay approved-but-unapplied with a note), reject records it. Approve is the only path to apply + the protected-fields guard is re-asserted, so nothing risk/secret/live can be applied from the UI. Two review rounds (fixed: uncaught DB-busy dropping the socket; stored-XSS via model text — added esc() across all innerHTML model-text renders))_
- [x] Surface all controls already in `Controller` (start/stop/pause/resume/close_trade/reset) plus a per-AI enable/disable toggle. _(start/stop/pause/resume/reset/close already in the UI; added a runtime per-strategy enable/disable toggle — Controller._disabled set + set_strategy_enabled + is_disabled callback into the engine; disabled strategy still gets stop/liquidation risk management but takes no new decision (AI skips the consult); POST /api/toggle + per-card button. Runtime-only (resets on restart))_
- [x] Daemon hardening: confirm `daemon.run_daemon` auto-restarts on crash with backoff (`daemon_backoff_seconds`), writes `daemon_status.json`, and the clean SIGTERM/SIGINT shutdown (already landed) is covered by `test_daemon.py`. _(all present; hardened the restart-backoff sleep to be interruptible (stop_event.wait, not time.sleep) so SIGTERM during backoff stops promptly; added injectable stop_event; test_daemon now covers backoff value, stop-during-backoff, and the restart→stopped status transition)_
- [x] Always-on: document + script an OS-level supervisor (launchd plist on macOS / systemd unit) so the daemon restarts on reboot; health-ping `#comms` via `comms.post` on start/stop/crash. _(homing_trade/supervisor.py renders launchd plist (RunAtLoad+KeepAlive) + systemd unit (Restart=always, WantedBy=multi-user.target), baking in the venv python + repo dir; CLI `python -m homing_trade.supervisor --kind …`; docs/always-on.md install/uninstall + the two-layer model. start/stop/crash health-ping already emitted via the daemon notifier when alert_mode=discord)_
- [ ] Wire `comms.read` (bot-token inbound) so approvals can also arrive from Discord, not only the web UI (depends on the Discord bot token — see API shopping list). _(BLOCKED on the Discord bot token; webhook is one-way)_
- [x] Tests: extend `test_web.py` for the new endpoints + approval queue; `test_daemon.py` for restart/backoff. _(test_web covers leaderboard/brain-log/regime+exit breakdown/toggle endpoint + state; test_daemon covers restart/backoff/stop-during-backoff/status; test_supervisor covers config rendering. The approval-queue UI test lands with #4 in Phase 4)_

Progress: 8/9 _(leaderboard + AI brain-log + per-regime/per-exit breakdown + proposal-queue UI (approve→apply) + per-strategy toggle + daemon hardening + always-on OS supervisor live; trade_outcomes rebuild wired into the engine loop; new endpoints/daemon/supervisor all tested. The only remaining box is externally blocked: #8 `comms.read` inbound → Discord bot token. Phase-3 work complete bar that dependency)_

---

## Phase 4 — The autonomous LEARN → CORRECT loop (the heart of the project)
Goal: the AI reflects over its own SQLite history, finds what went right/wrong, and PROPOSES adjustments; a human approves before anything applies. Nothing self-applies.

- [x] Add tables: `reflections(id, strategy, kind['per_trade'|'periodic'], ts, batch_from_ts, batch_to_ts, trade_ids_json, metrics_json, lesson, new_playbook_version, model, raw)` and `playbooks(version PK, strategy, created_ts, rules_json, parent_version, retired_ts)` (append-only; never UPDATE a published version). _(migration v7; both model-authored; accessors record_reflection/recent_reflections + publish_playbook/latest_playbook/get_playbook/retire_playbook — retire only sets retired_ts, rules_json never mutated; real-DB v6→v7 verified)_
- [x] Add `proposals(id, strategy, kind['param'|'prompt'|'playbook'|'strategy_toggle'], payload_json, rationale, status['pending'|'approved'|'rejected'], created_ts, decided_ts, decided_by, source_reflection_id)` — the gate between AI suggestion and applied change. _(migration v8; create_proposal records pending + a protected-fields guard (denylist from real config + risk/secret family substrings + value-scan for param) that refuses to even propose risk limits/kill-switch/leverage/risk_pct/stop_pct/live-arming/secrets; decide_proposal only flips pending→approved/rejected (idempotent), applies nothing. proposals classified model-authored. Adversarial review caught+fixed leverage_min/risk_pct/stop_pct gaps. Unblocks Phase-3 #4)_
- [x] Build `reflection.py`: a wall-clock-paced (decoupled from the candle loop, like the AI poll cadence) batched retrospection over `trade_outcomes`, producing a compact lesson + a proposed playbook diff. Use it as the PRIMARY loop. _(ReflectionEngine.run_once: embargo (as_of) + watermark (batch_to_ts) batching, mechanical SelfQuery metrics in the prompt with an explicit "don't re-grade" guard, injectable LLM (degrades to no-op, never crashes the loop), records a reflection + files a human-gated kind='playbook' proposal; never publishes/applies. NOW WIRED LIVE: reflect_runner.ReflectionRunner runs run_once per AI strategy on a slow wall-clock cadence (reflection_poll_sec, default 3600s, default-OFF) inside the engine's run_tick; build_reflect_fn(cfg) supplies the Claude backend (cli/api), scoped to AI traders (the only playbook consumers))_
- [x] Add a per-closed-trade Reflexion pass (one extra LLM call at CLOSE) that critiques the original observation/prediction/rationale vs realized P&L and `prediction_correct`; store as a `per_trade` reflection (secondary loop — never the only mechanism). _(ReflectionEngine.reflect_on_trade: joins the closed trade_outcome → its originating decision (decision_id) → the AI's observation/prediction/rationale, presents the MECHANICAL outcome (realized P&L/pnl_pct/prediction_correct/MAE/MFE/exit_reason) with an explicit "do NOT re-grade" guard, records a kind='per_trade' reflection. Files NO proposal (the periodic loop owns those). Embargo-aware (as_of), idempotent (one critique per position — bails before the LLM call), never crashes (no-model/error/garbled/empty/non-string-lesson all no-op). Also fixed cross-loop isolation: run_once's watermark now reads kind='periodic' only, so a per_trade row can't trigger re-reflection. db join helpers get_decision/llm_response_at/per_trade_reflection_exists + recent_reflections kind filter. Close-path cadence wiring is a follow-up slice)_
- [x] Score `prediction_correct` MECHANICALLY (did price do what `prediction` said over the horizon?), computed from candles — never by asking the model if it was right (reward-hacking guard). _(`rebuild_trade_outcomes` computes `prediction_correct` from the entry→exit price direction (LONG wants exit>entry, SHORT exit<entry) — purely from the trade's own prices, never the model's self-assessment; surfaced via `SelfQuery.directional_accuracy` and presented to BOTH reflection loops as ground truth with an explicit "do NOT re-grade" guard. Scope note: scoring is the directional bet outcome (a robust proxy), NOT NLP-parsing the free-text `prediction` string vs the price path over a stated horizon — the latter is intentionally avoided as brittle and reward-hackable. The invariant the item protects (mechanical, from prices, never self-graded) holds)_
- [x] Implement playbook injection in `llm_trader._build_context`: inject the current bounded (top-K) playbook rules + bump `prompt_version`; refine (supersede stale rules), do not blindly append. _(LlmTrader.playbook_provider (wired by SkillRunner to ledger.latest_playbook(name), per-trader) → _current_playbook() parses rules_json, keeps non-blank string rules, bounds to top-K (12); _build_context injects ctx["playbook"] + _SYSTEM frames them as human-approved priors. Emits prompt_version (mtf-v1 ⊕ playbook_version), playbook_version, prompt_hash in Signal.meta → process_tick persists them to llm_responses + decision_log. Degrades to the base prompt (no version claimed) on any read failure; never crashes the consult; injects only ALREADY-published (human-approved) playbooks. Refine-not-append is enforced upstream in reflection. The apply step (publish on approve) is #7)_
- [x] Confidence calibration report: per-confidence-band realized win rate per strategy, feeding a proposed confidence floor (a `proposals` row). _(SelfQuery.confidence_calibration buckets completed trades by their ENTRY confidence (trade_outcomes ⋈ decision_log via decision_id) → per-band n/win_rate(mechanical)/pnl, embargo-aware + read-only. calibration.propose_confidence_floor files a human-gated param proposal {confidence_floor} when low bands underperform target (default 0.5) and a higher band clears it; idempotent (any pending floor blocks a new one); PROPOSE-only. Wired into the ReflectionRunner cadence (mechanical, no LLM). confidence_floor has no runtime consumer yet — wiring it into the decision gate is a follow-up)_
- [x] All proposals require human Approve (web UI Phase-3 or `#comms` reply); on approve, apply param/prompt/playbook change and record the new version. NEVER auto-touch risk limits / kill-switch / secrets / live-arming. _(homing_trade/proposals.ProposalApplier.apply — the ONLY place an approved proposal becomes effective: applies only an approved+not-yet-applied row (idempotent), RE-ASSERTS the protected-fields guard at apply time (defense in depth — even an out-of-band approved protected payload can't be applied), records applied_ts/by/result (schema v9). playbook kind publishes the new immutable version + supersedes the current one atomically (one txn: publish+retire+mark, true-lineage parent) — closing the loop with the #6 injection. param/prompt/strategy_toggle are explicitly REFUSED until their runtime override stores exist (no silent no-op). Two review rounds (two MAJORs fixed: non-atomic stranding, strategy=None misreport). Unblocks Phase-3 #4 proposal-queue UI)_
- [x] Per-rule and per-playbook-version performance slope tracking; auto-surface (as a proposal) a rollback when a playbook version degrades. Disconfirmation guard: flag beliefs the bot stopped testing. _(SelfQuery.playbook_performance attributes completed trades to the playbook_version active at entry (trade_outcomes ⋈ decision_log via decision_id), embargo-aware + read-only. playbook_rollback.propose_rollback files a human-gated proposal carrying the parent's rules when the current version is worse on BOTH avg_pnl and win_rate (>= min_trades each) — the apply step publishes+supersedes; idempotent; guarded against oscillation (never rolls back a version that is itself a rollback). SelfQuery.disconfirmation_flags surfaces regimes the current version stopped trading that its parent did (beliefs no longer being tested). Wired into the ReflectionRunner cadence. Per-RULE attribution is intentionally not done — a rule is free text with no firing-instrumentation; per-version is the tractable unit)_
- [x] Tests: `test_reflection.py` (batching, embargo respected, mechanical scoring), `test_proposals.py` (nothing applies without approval; protected fields can never be proposed). _(both present + comprehensive: test_reflection covers batching/embargo/watermark/never-crash/no-self-grade + the per-trade Reflexion; test_proposals covers the protected-fields guard (config-seeded coverage, can-never-be-proposed) + the decide flow; test_proposal_apply covers nothing-applies-without-approval + atomic apply + protected re-check. Plus test_reflect_runner/test_calibration/test_playbook_rollback/test_playbook_injection across the Phase-4 PRs. Full suite 407 green)_

Progress: 10/10 _(reflections + append-only playbooks (v7) + proposals approval-gate (v8) + reflection.py (primary batched loop) + reflect_on_trade (secondary per-trade Reflexion) + mechanical prediction_correct (#5) + playbook injection with prompt/playbook-version provenance (#6) + the APPLY step (#7, ProposalApplier — atomic publish/supersede on human-approve, protected re-check, applied provenance v9) + confidence calibration → confidence-floor proposals (#8) + per-playbook-version slope → auto rollback + disconfirmation guard (#9) + comprehensive tests (#10) all landed. **Phase 4 complete.** The autonomous reflect→propose→approve→apply→inject loop is closed end-to-end for playbooks AND runs LIVE on the engine cadence (run_once + calibration + rollback proposers fire per AI strategy on a slow wall-clock poll, default-OFF, all human-gated). Two deliberate follow-ups remain (not Phase-4 boxes): the per-closed-trade Reflexion (reflect_on_trade, #4) still needs its close-path wiring, and the confidence_floor (#8) needs a decision-gate consumer)_

---

## Phase 5 — Multi-AI provider registry
Goal: any model behind `AI_<NAME>_*` env flags — whitelisted, each its own wallet+brain, with per-provider cost accounting.

- [x] Generalize `ai_traders.build_ai_traders` from the two hard-coded brains (`llm_claude_code`, `llm_anthropic`) to a registry that discovers `AI_<NAME>_IS_ENABLED` / `AI_<NAME>_POLL_IN_SEC` / `AI_<NAME>_BACKEND` / `AI_<NAME>_MODEL` env flags. _(build_ai_traders now merges the two built-ins (driven by their typed Config fields, back-compat — explicit env poll/model wins, a bare enable flag keeps the Config value) with any provider discovered generically from `AI_<NAME>_*`; each spins up as `llm_<name_lower>` with its own wallet/backend/cadence/model. Unsupported backends (outside cli/api — adapters are #2) are SKIPPED not mis-routed; the approved-name whitelist is #3. from_env captures the AI_* env subset into `Config.ai_providers_env` and the registry discovers from that snapshot (never the live os.environ) so engine/backtest/test composition is deterministic. Adversarial review fixed two MAJORs (bare-flag clobbering Config poll/model; ambient-env read) before merge. test_ai_traders 17 green; suite 420)_
- [x] Backend adapters behind a common interface: `cli` (Claude CLI), `api` (Anthropic), plus OpenAI, Mistral, Llama/local — each optional, degrading to HOLD if its SDK/key is absent (mirror `llm_trader`'s never-crash contract). _(new leaf module `homing_trade/llm_backends.py`: a `BackendRequest` dataclass + a `BACKENDS` registry of adapters behind one interface `decide(backend, req) -> (decision_dict, raw_text)`. Backends cli/api/openai/mistral/llama (llama = local / OpenAI-compatible endpoint via `LLAMA_BASE_URL`). LlmTrader dispatches through it (cli+api call shapes preserved byte-for-byte; `_extract_json` moved to the leaf + re-exported). Each provider SDK is imported lazily, so an absent library/key/server degrades to HOLD via the unchanged on_candle try/except. ai_traders.KNOWN_BACKENDS = set(BACKENDS), so an openai/mistral/llama provider now spins up a brain; a genuinely unknown backend is still skipped at discovery. No import cycle (leaf imports nothing from homing_trade). test_llm_backends 12 (injected fakes assert the wire kwargs + HOLD-on-error/missing-SDK); suite 433. Review: SHIP)_
- [x] Whitelist enforcement: only registered/approved provider names spin up a wallet+brain. _(security gate in ai_traders: `approved_names(env)` = the always-approved built-ins (CLAUDE_CODE/ANTHROPIC) ∪ the operator's `AI_PROVIDERS_WHITELIST` (comma-separated, case-insensitive). `discover_providers` drops any enabled `AI_<NAME>_IS_ENABLED` whose name isn't approved — checked BEFORE the backend gate, so a stray flag can never mint a brain even with a valid backend. Reads the deterministic AI_* env snapshot; the built-in Config-field path only ever adds the two always-approved built-ins, so the gate is unbypassable. .env.example documents it. test_ai_traders +5 whitelist tests (incl. the non-tautological unapproved-name-skipped negative); suite 438. Review: SHIP)_
- [x] Per-provider cost accounting: a `cost_ledger(strategy, ts, model, prompt_tokens, completion_tokens, usd)` table; the leaderboard shows tokens and $ per provider. _(schema v10 `cost_ledger` (+ backend col), classified AUDIT-TRUTH — machine-written, never model-authored. llm_backends adapters now return (decision, raw, usage); MODEL_PRICES + estimate_usd give best-effort USD (longest-substring match, None for unknown/local — usd NEVER fabricated, tokens still recorded), and the Claude CLI's authoritative total_cost_usd is used verbatim. process_tick records one row per real consult with usage (never on error/waiting-HOLD); usage extraction is inside on_candle's never-crash try so a bad usage object degrades to HOLD. SelfQuery.cost_summary (read-only rollup) + build_state surfaces per-AI calls/tokens/$. v9->v10 verified on a real DB copy (integrity ok, 11 trades intact). test_cost_ledger + extended llm_backends/web/hierarchy tests; suite 448. Review: SHIP. The dashboard cost COLUMN render is #5)_
- [x] UI: per-AI cost column + enable/disable toggle (extends Phase-3 leaderboard). _(each AI card's metrics grid now shows an "AI cost" row — total $ + tokens, with a hover title breaking out consults + in/out tokens — fed by build_state's x.cost (the #4 cost_ledger rollup). New `usd` formatter (sub-dollar 4dp, ≥$1 2dp, null→—); guarded by x.cost?…:'' so non-AI cards are unchanged. The enable/disable toggle already sat on every card (Phase 3). Cost values are numeric SQL aggregates (never model-authored) through fmt/usd, so no new injection surface. node --check passes; test_dashboard_renders_per_ai_cost_column guards the wiring. suite 449. Review: SHIP)_
- [x] Tests: `test_ai_registry.py` (env discovery, whitelist, unknown/missing-key degrades cleanly). _(end-to-end suite walking the real operator path: AI_<NAME>_* env → from_env (AI_* snapshot) → build_ai_traders → whitelist gate → backend gate → a configured LlmTrader → on_candle degrades to HOLD when the SDK is absent. Each gate has a working positive control (whitelisted-generic spins up, identical-but-unapproved does not, whitelisted+unknown-backend does not); the degrade test provably hits the absent-SDK path (monkeypatched __import__). Unit coverage stays in test_ai_traders/test_llm_backends. Review: SHIP)_

Progress: 6/6 _(**Phase 5 COMPLETE.** env-discovered provider registry (#1, PR #50) + backend-adapter interface cli/api/openai/mistral/llama (#2, PR #51) + provider-name whitelist gate (#3, PR #52) + per-provider cost accounting cost_ledger v10 (#4, PR #53) + dashboard per-AI cost column (#5, PR #54) + the end-to-end registry test suite (#6, PR #55) all landed. Any whitelisted model behind AI_<NAME>_* env flags now runs as its own wallet+brain, each backend degrading to HOLD if its SDK/key is absent, with tokens + $ tracked per provider. 456 tests green)_

---

## Phase 6 — External research ingestion
Goal: news/sentiment/derivatives/on-chain signals into the AI context, and an internet scan that FILES new algorithms as candidate strategies (never auto-trading).

- [x] `signals/fng.py`: Alternative.me Fear & Greed (free, no key) → cached table, injected into `llm_trader` context. Wire first. _(fetch_fng (degrade to None on any error) + cache-aware get_fng (serve-fresh / refetch-stale / fallback-to-stale-or-None); injectable fetcher (offline tests). Generic schema-v11 signal_cache(source,key,ts,value_json,fetched_at) — AUDIT-TRUTH, reused by the rest of Phase 6. LlmTrader.fng_provider (wired by SkillRunner when fng_enabled + ledger has a cache) injects ctx["fear_greed"]; _SYSTEM frames it as a contextual gauge (never a standalone trigger); PROMPT_VERSION mtf-v1→mtf-v2. Default ON (keyless); backtests (MemoryLedger) never wire it → no network in tests. v10→v11 verified on a real DB copy. test_signals_fng 13 green; suite 469. Review: SHIP. Cold-cache synchronous fetch noted as #6 hardening)_
- [x] `signals/derivs.py`: Binance (then OKX/Bybit) public funding-rate + open-interest; aggregate cross-venue funding skew. No key needed. _(fetch_binance (funding + mark + OI; OI best-effort) + a _VENUES tuple OKX/Bybit append to + funding_skew (cross-venue mean/spread) + fetch_derivs (aggregate, None if no venue) + cache-aware get_derivs (serve-fresh/refetch-stale/fallback). Reuses signal_cache (source='derivs', key=symbol) — no schema change. Injected into the AI context as positioning (never a standalone trigger) via a generalized LlmTrader context-provider registry (add_context_provider — the single fng_provider became one entry; all Phase-6 feeds plug in here). PROMPT_VERSION mtf-v2→mtf-v3. Per-trader by its own Binance symbol (no late-binding). Default ON (keyless); backtests never wire it. test_signals_derivs 13 green; suite 482. Review: SHIP. OKX/Bybit venues are the noted follow-on)_
- [x] `signals/coindcx.py`: CoinDCX public futures funding/mark/orderbook for the actual traded instrument (source of truth). _(parse_orderbook (best bid/ask, mid, spread_bps, top-5 depth imbalance) + fetch_futures_rt (best-effort mark/funding, defensive shape parse) + fetch_coindcx (combine, None only if BOTH fail) + cache-aware get_coindcx. Reuses signal_cache (source='coindcx', key=pair) — no schema change. Injected via the shared context-provider registry as the execution source of truth (spread = cost, imbalance = pressure; overrides cross-ref venues on price); PROMPT_VERSION mtf-v3→mtf-v4. Per-trader by its own pair; default ON (keyless); backtests never wire it. test_signals_coindcx 12 green; suite 494. Review: SHIP)_
- [x] `signals/price_ref.py`: CoinGecko Demo reference price/market context to sanity-check CoinDCX (needs Demo key). _(fetch_price_ref (BTC+ETH usd + 24h change + market cap in one call) + resolve_key (free Demo key from the gitignored env var) + cache-aware get_price_ref. Works KEYLESS on the public tier; the Demo key (sent only as the x_cg_demo_api_key param — never logged/stored/committed) just lifts the rate limit. Injected as a venue sanity-check (gap vs venue mid/mark = caution, not a trigger); PROMPT_VERSION mtf-v4→mtf-v5. Reuses signal_cache (source='price_ref', key='usd'). Default ON; backtests never wire it. test_signals_price_ref 10 green; suite 504. Review: SHIP, secret path traced clean)_
- [x] Optional `signals/news.py`: RSS-into-LLM as the default news feed (CryptoPanic free tier retires 2026-04-01; do not hard-depend on it). _(pure free RSS — CoinDesk + Cointelegraph, NO CryptoPanic (its free tier already retired). parse_rss (stdlib ElementTree, no feedparser dep) + fetch_news (multi-feed, per-feed best-effort, case-insensitive dedup, bounded) + cache-aware get_news. Injected as macro/event context (awareness of a catalyst, never a standalone trigger); PROMPT_VERSION mtf-v5→mtf-v6. Reuses signal_cache (source='news'). XML surface assessed (no XXE/SSRF in stdlib; billion-laughs accepted for publisher-HTTPS feeds on a degrade-safe read, documented in-code). Default ON (keyless); backtests never wire it. test_signals_news 12 green; suite 516. Review: SHIP)_
- [x] Cache all external pulls in SQLite with `fetched_at`; respect rate limits; every fetch path degrades to "signal unavailable" without crashing the loop. _(signals/cache.py::cached_signal — the ONE enforced path all five feeds now delegate to: caches every pull in signal_cache with fetched_at, serves a fresh cached value (≤1 refetch per max_age window; the freshness window + the slow hourly consult cadence ARE the rate-limiter), and degrades to the stale value or None on any failure (wraps fetch_fn in try/except — never raises). The five get_* were consolidated byte-for-byte behavior-preserving (per-feed ts matrix verified). db.all_signals() + signal_status() make the cache inspectable; build_state surfaces it + a dashboard "research signals" freshness panel. test_signal_cache 9 green; 60 existing signal tests unchanged; suite 527. Review: SHIP)_
- [x] Candidate-strategy intake: an internet/research scan that writes new algorithm ideas as `proposals(kind='strategy_toggle')` rows for human approval — never auto-enabled. _(research.py: StrategyResearcher parses candidates from an injectable research_fn → files each NEW one as a strategy_toggle proposal (payload KEYS bot-fixed to {strategy,action,description} so a candidate supplies only values — can't inject a protected key; the create_proposal guard still applies). Idempotent vs pending names + within-batch; degrade-safe (no fn/error/garbled → no-op, never raises). NEVER auto-enables — ProposalApplier refuses strategy_toggle apply even on approve (verified against the real applier). ResearchRunner runs it on a daily cadence, gated default-OFF (build_research_fn None unless research_enabled). Extracted the shared cli/api text adapter (llm_text.py) — reflect_runner now delegates to it (behavior-preserving). test_research 11 green; suite 538. Review: SHIP)_
- [x] Tests: `test_signals_*` with injected fetchers (offline, deterministic). _(The per-feed suites inject fetchers — so they never exercise the REAL requests-based default path. PR #63 adds tests/test_signals_offline.py: the cross-cutting offline guard. It HARD-BLOCKS the network (socket.getaddrinfo + socket.connect raise → egress fails fast, no 10s timeout) and drives every default fetch_*/get_* through its real fetcher, locking in: every fetch_*() degrades to None (no raise/hang); every cache-aware get_*(repo) with a cold cache returns None AND writes NOTHING to signal_cache (a failed pull can't poison the cache); test_block_is_real proves the block genuinely raises so the degrade asserts pass because the code degrades. Adversarial review: block verified real+complete (instrumented per-feed egress counts), no test passes for the wrong reason, real _http_fetcher exercised, no flakiness; only a docstring NIT, fixed. Suite 538→545 green. Review: SHIP)_

Progress: 8/8 ✅ COMPLETE _(#1–#5 the five external feeds (PRs #56–#60) + #6 the one enforced cache contract + observability (PR #61) + #7 candidate-strategy intake → human-gated strategy_toggle proposals (PR #62) + #8 the cross-cutting offline degrade guard (PR #63) all landed. Phase 6 done: news/sentiment/derivatives/on-chain/ref-price all feed the AI context through one cached, degrade-safe, rate-limited signal_cache contract; an internet research scan files candidate algorithms as human-gated strategy_toggle proposals (never auto-trading); the whole signals layer is proven network-isolated and degrade-safe under a hard offline block. All feeds default-ON + keyless + free; research + reflection LLM loops default-OFF/unbilled. Next: Phase 7 — honest walk-forward backtests)_

---

## Phase 7 — Profitability via honest walk-forward backtests (continuous)
Goal: continuous, honest walk-forward evaluation. It is OK to lose paper money while learning; the discipline is what matters.

- [x] Walk-forward harness on top of `backtest.run_backtest`: train window → freeze params/prompt/playbook → evaluate on the next unseen window → roll forward. A change is "learned" only if it holds out-of-sample. _(PR #64: homing_trade/walkforward.py. fold_bounds tiles contiguous non-overlapping in-bounds OOS test windows (step defaults to test); walk_forward(skill_factory, candles, cfg, balance, *, train, test, step, window, fit_fn) returns per-fold OOS results + a compounded aggregate (compounded_return/hit_rate/mean_sharpe/worst_drawdown/worst_fold). NO LOOKAHEAD: a fold evaluates only candles[te_lo:te_hi]; fit_fn sees only its train slice — locked in by a test mutating ALL candles after fold0's test window asserting fold0 bit-identical (+ a non-vacuous check the mutation moved a later fold). fit→freeze→test gate: fit_fn(skill, train, cfg)→frozen cfg applied to test eval, so an in-sample fit can't inflate OOS (default fit=identity; mechanical strategies have no tunable params yet — this is the hook a future tuner plugs into). skill_factory MUST be callable (fresh skill/fold) — bare instance rejected so a stateful skill can't bleed state across folds. Thin CLI `python -m homing_trade.walkforward`, smoke-tested on real candles. Documented conservative caveat: indicators warm up within each test slice (never a leak). Adversarial review: lookahead/fold-math/aggregate correct; one state-bleed footgun fixed+regression-tested. Suite 545→556 green. Review: SHIP)_
- [x] Add the shortlisted strategies as skills (see candidate algos): Supertrend, volume-confirmed breakout, TTM Squeeze, regime filter, z-score reversion — each with its own `test_<skill>.py`. _(5/5 — the 4 trading candidates as skills + the regime filter delivered as the #3 gate (it's a gate, not a standalone skill). — PR #65: Supertrend (ATR-banded trend follower, canonical final-band recursion, trades the flip, ≥3-bar confirmed reference) + z-score reversion (rolling-window z). PR #66: volume-confirmed breakout (Donchian break gated on volume ≥ vol_mult×avg, zero-vol guarded) + TTM Squeeze (BB-inside-KC compression, trades the release by momentum, prev-state from candles[:-1] so no look-ahead). Plus a reusable indicators.atr()/true_ranges(). All registered in _SKILL_FACTORY but NOT in enabled_skills — candidates, evaluable by backtest/walk-forward, promoted only out-of-sample. Each has its own test_<skill>.py; all verified end-to-end through the walk-forward harness on real candles. Adversarial review each PR: recursion/flip/breakout-slice/BB-inside-KC/no-lookahead/no-auto-enable confirmed; findings fixed. Suite →582 green. Remaining: regime filter — folded into #3 (it becomes the allocator/committee gate))_
- [x] Regime filter as a gate/weight feeding the allocator + committee (highest portfolio-level leverage). _(PR #67: regime_filter.py turns the per-tick classify_regime label into the portfolio gate. regime_weight(name, regime) multiplies each strategy's allocator weight — trend styles favored in trends / cut to regime_unfavored_weight in chop, reverters the reverse; committee_threshold_scale(regime) raises the committee's consensus bar in non-trending regimes. Conservative by construction: NEUTRAL style or an ambiguous regime (unknown/transition/None) is never penalized and NO multiplier exceeds 1.0 — the gate only reduces exposure / raises the bar, never invents conviction. Wired into process_tick (scales weight + sets committee._regime_threshold_scale each tick; clears a stale scale on a flag flip-off). Hard-risk exits (risk veto/stop/liquidation) unaffected — only the discretionary entry bar moves. Default-OFF (byte-for-byte unchanged when disabled). All 3 regime_* config fields added to PROTECTED_PROPOSAL_FIELDS (human-gated, never model-auto-tuned). This also absorbs the last #2 candidate — the regime filter is a gate, not a standalone skill. Adversarial review: never>1 / backward-compat / CLOSE-not-blocked / protected-fields confirmed; 1 MINOR fixed (stale-scale flip). Suite 582→596 green. Review: SHIP)_
- [x] A/B variant bookkeeping: `experiments(id, hypothesis, variant_a, variant_b, metric, mde, start_ts, end_ts, n_a, n_b, result, p_value, correction_method)`; record search budget for honest multiple-testing accounting. _(PR #68: db.py v12 experiments ledger (exactly those columns) + create/conclude/get/list_experiments + experiment_search_budget (the multiple-testing denominator). Audit-truth: hypothesis is a definitional label, not model prose. Plus experiments.py — a pure-stdlib two-sample stats engine: Welch's t + Welch-Satterthwaite df + an EXACT two-sided p-value via the regularized incomplete beta (not a normal approx, so honest for small samples) + min_detectable_effect. Status keys off end_ts (the conclusion marker) so an inconclusive result is still 'concluded'. v11→v12 verified on a real-DB copy (integrity ok, 11 trades intact). Adversarial review: p-values exact to ~1e-15 vs t-table + df=1/2 closed forms; MDE/guards/migration/SQL-safety confirmed; 1 MINOR fixed (status from end_ts). Tests anchor EXACT p-values. Suite 596→611 green. Review: SHIP)_
- [x] Promotion discipline: a variant is promoted only after it wins out-of-sample AND clears MDE with multiple-comparison correction (Bonferroni / BH) — surfaced as a `proposals` row, human-approved. _(PR #69: promotion.py. bonferroni_alpha + benjamini_hochberg (step-up FDR); promotion_decision — the pure 4-condition gate (enough samples ∧ won OOS ∧ clears MDE ∧ significant) using the ONE-sided directional p; evaluate_and_maybe_promote orchestrates OOS Welch test → conclude the experiment → file a human-gated strategy_toggle proposal iff the gate passes. Search-budget family = experiments started in a rolling 90d window (bar can't tighten to impossibility). Crash-safe + idempotent: files BEFORE concluding; skips a duplicate pending promotion + one rejected within a 7d cooldown. NEVER auto-enables (ProposalApplier refuses strategy_toggle — proven by test). Adversarial review: Bonferroni/BH correct vs reference, two-sided→one-sided sound, safety airtight; 4 MINORs fixed (rolling window, one-sided, file-before-conclude, reject cooldown) + end-to-end Bonferroni-blocks test. Suite 611→629 green. Review: SHIP)_
- [x] Guard against the LLM "profit mirage": only trust backtests on post-cutoff, walk-forward data. _(PR #70: profit_mirage.py — a result is TRUSTED only when its window is entirely after the model knowledge cutoff AND walk-forward. cutoff_ms_from_iso (UTC; FAILS CLOSED on an invalid non-blank string so a typo can't silently disable the guard); is_post_cutoff keys off the window start (conservative for a straddling window); assess_window (post-cutoff ∧ walk-forward ∧ well-formed). Wired into walk_forward (per-fold post_cutoff tag + a trusted_oos aggregate over only post-cutoff folds — the mirage becomes visible: oos-all vs trusted; CLI --cutoff) and into the promotion gate (optional oos_window/cutoff_ms refuses to promote a pre-cutoff window, even a significant one — concludes 'untrusted', files nothing). config.trust_cutoff_iso (default 2026-01-01) added to PROTECTED_PROPOSAL_FIELDS. Default cutoff=None is purely additive (prior behavior preserved). Adversarial review: TZ-correct, straddle conservative, backward-compat verified; 1 MAJOR fixed (was fail-OPEN on invalid cutoff → now fail-CLOSED) + None-start/malformed-window guards. Suite 629→640 green. Review: SHIP)_
- [x] Continuous backtest job (cron/daemon) writing results to SQLite, surfaced in the UI. _(PR #71: db v13 backtest_results ledger (OOS + trusted/post-cutoff metrics per run, audit-truth) + record/recent/latest_per_strategy. backtest_job.py BacktestRunner — cadence-gated default-OFF, self-paced (daily), isolated, pulls ONLY stored candles (no network in the tick), fetches candles before stamping the cadence (a transient empty read can't burn a day). Wired into the engine tick (constructed once, self-gated) via a lazy import that breaks the engine↔job cycle; CLI `python -m homing_trade.backtest_job` for cron. build_state surfaces latest_backtest_per_strategy + a dashboard "walk-forward backtests" panel (OOS vs trusted). v12→v13 verified on a real-DB copy. Adversarial review: cadence/isolation/network/schema/cycle confirmed, no MAJOR; findings fixed (stamp-after-candle-read, drop redundant build, non-vacuous trusted/empty-folds tests). CLI smoke shows the guard live (ma_trend OOS −16.5% vs trusted −4.1%). Suite 640→651 green. Review: SHIP)_

Progress: 7/7 ✅ COMPLETE _(#1 walk-forward OOS harness (#64); #2 the 5 candidates (#65–66 + the #3 gate); #3 regime-aware portfolio gate (#67); #4 A/B experiments ledger + exact stats (#68); #5 OOS promotion gate → human-gated proposal (#69); #6 profit-mirage trust guard (#70); #7 continuous backtest job → SQLite → UI (#71). Phase 7 done: an end-to-end honest walk-forward evaluation pipeline — roll train→freeze→OOS-test, a regime gate sizing the portfolio, A/B experiments with exact two-sample stats, a promotion gate demanding an out-of-sample win that clears MDE + multiple-comparison correction (human-gated, never auto-enabled), a profit-mirage guard trusting only post-cutoff data, and a continuous backtest job feeding it all into SQLite + the dashboard. Profitability stays the north star but is NOT a near-term gate — losing paper money while the loop learns is fine. Next: Phase 8 — GitHub project-management visibility)_

---

## Phase 8 — GitHub project-management visibility
Goal: the board, this ROADMAP, and the PRs always agree; PRs are self-explanatory because the user merges on trust.

- [ ] Mirror these phases onto a GitHub Project board (NeuronEnix/homing-trade); one card per task.
- [ ] Keep `Progress: x/N` lines accurate on every merge; CI check fails if ROADMAP boxes and board drift (optional later).
- [ ] PR template: what/why, what's tested, screenshots of UI changes, and an explicit "does NOT touch risk limits / kill-switch / secrets / live-arming" line.
- [ ] CI: run the full test suite on every PR; block merge on red.

Progress: 0/4

---

## Phase 9 — Self-modifying codebase (gated)
Goal: the AI proposes CODE changes as branches/PRs with backtests + passing tests; a human merges. Never touches protected zones without explicit sign-off.

- [ ] AI proposes code changes only as a branch + PR (via `gh`), with attached walk-forward backtest results and green tests.
- [ ] Hard guardrail: a protected-paths denylist (risk limits, kill-switch, secrets handling, live-arming, `LiveBroker` dry-run flag) that proposals can never modify.
- [ ] Every self-modification PR must pass CI before a human can merge; no auto-merge.
- [ ] Provenance: link each PR back to the `reflections`/`proposals` row that motivated it.

Progress: 0/4

---

## Phase 10 — Real-money test-drive milestone (opt-in, kill-switched)
Goal: after a paper track record, a tiny opt-in live run behind an explicit human gate, then gradual scale-up.

- [ ] Explicit human arming gate flips `live_enabled`/`live_dry_run` in `live_broker.py`; default stays dry-run.
- [ ] Tiny capital cap + hard `max_daily_loss` kill-switch verified live before any scale-up.
- [ ] Gradual scale-up only after a sustained paper + tiny-live track record.

Progress: 0/3

---

## Backlog / Later (low priority)
- [ ] Config versioning framework (`ConfigV1/V2` + migrator) — `config.py` has ~50 fields.
- [ ] `ErrorBoundary` abstraction (per-skill error counters, auto-disable after N failures).
- [ ] Decision/candle deterministic replay tool for trade-by-trade audit.
- [ ] Layered memory (short/mid/long/reflection) with temporal-decay retrieval, once trade count is large.
- [ ] Deferred algos needing new feeds/architecture: funding-rate skew (single-leg contrarian filter), rolling/anchored VWAP, CVD/order-flow (needs tick data), Ichimoku, BTC-ETH cointegration pairs (needs multi-leg positions).
- [ ] Paid data add-ons only if budget allows: Coinglass Hobbyist ($29/mo, aggregated funding + liquidation heatmaps).
- [ ] `StateSnapshot` versioned service for the UI if `build_state` schema churns.

---

_Design of record: `docs/superpowers/specs/2026-06-22-self-sustaining-trading-program-design.md`. Credentials to request: `docs/api-keys-shopping-list.md`. Two-way Discord approvals: `docs/discord-bot-setup.md`._
